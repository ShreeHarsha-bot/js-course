const name="Harsha"
const age=18

console.log(name+age +"Mandya");
//this is the old syntax which used to follow

//In recent days they will use the backticks(` `) for the string
//They use the backticks just bcz of the sting ineterpulation means inside that output statment only we can write the var name using the $ symbol
//example below

console.log(`My name is ${name} ,I am ${age} year old and I am from Mandya`);

//there are many built in function in the string like for example trim,replace,slice,subString,indexOf etc explore it

console.log(Math) //Object [Math] {} it is a object which contains many properties